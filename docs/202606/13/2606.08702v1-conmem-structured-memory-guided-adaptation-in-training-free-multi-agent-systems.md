---
title: "ConMem: Structured Memory-Guided Adaptation in Training-Free Multi-Agent Systems"
title_zh: ConMem：无需训练的多智能体系统中的结构化记忆引导自适应
authors: "Zhixun Tan, Qiang Chen, Tairan Huang, Xiu Su, Yi Chen"
date: 2026-06-07
pdf: "https://arxiv.org/pdf/2606.08702v1"
tags: ["query:mm-agent-mem"]
score: 9.0
evidence: 面向多智能体系统的结构化记忆引导自适应框架
tldr: "现有基于LLM的多智能体系统在自适应过程中面临噪声轨迹、记忆与技能关系建模不足以及依赖额外训练等问题。为此，提出ConMem框架，通过将历史交互提炼为结构化记忆卡并组织成关系感知图，实现无训练的跨经验协调。实验表明，ConMem在多个基准上超越现有方法，同时剪枝超过50%的候选并减少80%以上的规划开销，实现了高效轻量级的自适应。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.08702v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1427, \"height\": 390, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1449, \"height\": 686, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 1454, \"height\": 393, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 1441, \"height\": 324, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 705, \"height\": 356, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 1438, \"height\": 389, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 1038, \"height\": 2033, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 1040, \"height\": 2032, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-009.webp\", \"caption\": \"\", \"page\": 0, \"index\": 9, \"width\": 1041, \"height\": 2034, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.08702v1/fig-010.webp\", \"caption\": \"\", \"page\": 0, \"index\": 10, \"width\": 1040, \"height\": 2031, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.08702v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1466, \"height\": 1306, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.08702v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1555, \"height\": 246, \"label\": \"Table\"}]"
motivation: 现有方法受限于噪声轨迹、记忆与技能关系建模不足，且依赖额外训练或高质量监督，亟需一种无需训练的关系感知自适应框架。
method: ConMem将历史轨迹提炼为结构化记忆卡，构建关系感知记忆图；运行时按任务检索卡片，通过图协调解决策略冲突并恢复依赖，生成结构化指导。
result: "在多个基准和主流MAS架构上，ConMem一致优于现有方法，剪枝超过50%的扩展候选，规划开销减少80%以上。"
conclusion: ConMem提供了结构化、关系感知的指导，无需额外训练即可实现稳健、轻量级的多智能体自适应。
---

## 摘要
近期进展通过基于记忆、技能和学习的方法提升了基于大语言模型的多智能体系统（MAS）的自适应能力，但这些方法仍面临噪声轨迹、记忆-技能关系建模不足以及对额外训练或高质量监督依赖的挑战。为解决这些局限，我们提出ConMem，一种关系感知且无需训练的框架，通过跨经验协调实现高效的多智能体自适应。具体而言，ConMem将历史交互轨迹提炼为结构化记忆卡片以捕获可复用的策略和线索，并将其组织成关系感知记忆图。在运行时，ConMem根据任务需求检索卡片，并通过卡片图协调它们以解决策略冲突并恢复其依赖关系。这些模块结合产生结构化和关系感知的引导，使多智能体系统能够在不额外训练的情况下实现稳健、轻量级的自适应。跨多个基准和主流MAS架构的广泛实验表明，与现有记忆架构相比，ConMem取得了一致的性能提升，同时通过剪枝超过50%的扩展候选并减少80%以上的规划开销提高了推理效率。我们的代码可在https://anonymous.4open.science/r/ConMemCode获取。

## Abstract
Recent advances have improved the adaptive capabilities of LLM-based multi-agent systems (MAS) through memory-, skill-, and learning-based approaches, yet these approaches remain challenged by noisy trajectories, insufficient modeling of memory-skill relations, and reliance on additional training or high-quality supervision. To address these limitations, we propose ConMem, a relation-aware and training-free framework that enables efficient multi-agent adaptation through cross-experience coordination. Specifically, ConMem distills historical interaction trajectories into structured memory cards to capture reusable strategies and cues, organizing them into a relation-aware memory graph. At runtime, ConMem retrieves cards according to task needs and coordinates them through the card graph to resolve strategy conflicts and recover their dependencies. Combined, these modules yield structured and relation-aware guidance, enabling robust, lightweight adaptation in multi-agent systems without additional training. Extensive experiments across multiple benchmarks and mainstream MAS architectures show consistent gains over existing memory architectures, with improved inference-time efficiency through pruning more than 50% of expanded candidates and reducing planning overhead by over 80%. Our codes are available at https://anonymous.4open.science/r/ConMemCode

---

## 论文详细总结（自动生成）

### 1. 核心问题与整体含义（研究动机和背景）

- **问题**：现有基于大语言模型（LLM）的多智能体系统（MAS）在连续任务中依赖记忆、技能或学习机制实现自适应，但面临三大瓶颈：**噪声轨迹**（历史交互混杂工具结果、中间矛盾、过期上下文）、**记忆-技能关系建模不足**（仅按相关性召回，缺乏推理时关系感知协调，导致冲突、依赖和冗余未解决）、**对额外训练或高质量监督的依赖**（如强化学习记忆管理、策略优化，增加了计算复杂度和实施难度）。
- **动机**：能否**在主机模型冻结、不额外训练**的前提下，通过将碎片化经验转化为结构化的、可协调的集体记忆，实现高效的多智能体自适应？
- **整体含义**：将自适应视为**提示上的上下文控制**问题，而非模型权重更新。ConMem在冻结主机前，通过提炼可复用策略卡片、构建关系图、运行时检索/扩展/协调/组合，生成预算化的记忆前缀注入主机，实现轻量级、训练无关的自适应。

### 2. 方法论：核心思想、关键技术细节

- **核心思想**：以**结构化记忆卡**作为可复用策略原子单元，组织**类型化关系图**，通过**检索-图扩展-协调-预算化组合**管线，将历史经验转化为紧凑的、关系感知的提示引导，注入冻结的MAS主机。
- **关键技术细节**：
  - **记忆卡（Card）**：每个卡片`c = (z, κ, τ_c, χ)`，其中`z`描述任务，`χ`触发语义，`κ`包含四个字段：**state**（问题类型、前提条件）、**plan**（可复用策略或步骤）、**exec**（可泛化的执行模式）、**eval**（评估标准、失败教训）。卡片带**符号**（positive记录成功策略，negative记录失败警告）。
  - **记忆图（Graph）**：`G = (B, E)`，边类型`r ∈ {supports, constrains, satisfies, conflicts}`，权重`w ∈ [0,1]`。**supports/satisfies**为正向依赖边，**conflicts**为冲突边，**constrains**为条件限制边。
  - **写入管线** `W_Φ = Mrg ◦ Adm ◦ Agg ◦ Ref`：
    - **Ref**：对失败轨迹提取根因、错误行为、纠正做法、通用教训。
    - **Agg**：将轨迹压缩为候选卡片（成功轨迹直接提炼，失败轨迹后经Ref）。
    - **Adm**：基于质量评分`Q(c) = λ1C + λ2N + λ3R + λ4U`（可靠性、新颖性、近因性、预期效用）和一致性检查，阈值过滤。
    - **Mrg**：合并相似卡片，保持固定长度。
  - **读取管线** `U_Φ = PB_mem ◦ KG_t ◦ EG_t ◦ RB_t`：
    - **RB (Needs-aware Retrieval)**：估计任务需要剖面`π_t ∈ Δ³`，按多准则评分排名（语义相关、触发匹配、剖面对齐、可信度、质量），并用MMR去重。
    - **EG (Typed-Graph Expansion)**：从检索种子集出发，沿正向边（supports/satisfies）进行有界逐层扩展，冲突边阻断扩展，约束边需激活。
    - **KG (Coordinated Budgeted Composition)**：协调去除冗余、解决冲突（保留高质量代表），并按质量排序，在预算`B`内序列化卡片的紧凑表示。
  - **预算化上下文控制**：目标函数`max_K F_t(K) = U_t(K) - λ_conf Conf(K;G) - λ_red Red(K) - λ_stale Stale(K)`，约束总长度≤B。
- **关键公式**：公式(1)-(10)定义了检索评分、扩展、协调、组合的数学模型，包括需求估计遗憾界（定理2）、负向记忆激活阈值（公式5）、卡片充分性（定理1）等理论保证。

### 3. 实验设计：数据集/场景、基准、对比方法

- **数据集与任务**：共四个基准，覆盖三类任务：
  - **知识密集型QA**：TriviaQA（6993题）、PopQA（7267题）
  - **代码生成**：KodCode（2000题）
  - **符号规划**：PDDL via PDDLGym（60题）
- **MAS主机框架**（冻结主机）：AutoGen（分布内）、CAMEL、MacNet（未见主机）。所有主机使用相同LLM backbone：Qwen/Qwen3-4B-Instruct-2507，确定性解码。
- **对比方法**：
  - **无记忆基线**：No-memory
  - **智能体框架**：ChatDev、MetaGPT、JoyAgent、OAgents（自带记忆组件）
  - **训练无关记忆/技能**：Generative Agents、Voyager、G-Memory、SimpleMem、ReMe（动态）
  - **可学习记忆**：LatentMem（需额外训练）
- **指标**：QA用准确率，代码用单元测试通过率，PDDL用归一化规划得分。所有实验采用**预准序（prequential）** 协议：任务t仅使用t之前提交的卡片，轨迹在评分后才用于记忆更新。检索型QA使用固定外部检索器（Search-R1 stack）。

### 4. 资源与算力

- 论文**未明确说明**使用的GPU型号、数量、训练时长。
- 推理使用Qwen3-4B-Instruct-2507，为轻量级模型（4B参数）。ConMem本身无需训练，所有模块（卡片提取、关系判断、协调）均通过LLM调用或规则完成，因此算力消耗主要在推理和少量LLM辅助操作上。文末代码仓库可能包含更详细信息，但正文未提及具体硬件资源。

### 5. 实验数量与充分性

- **实验数量**：非常充分。
  - **主实验**（表1）：3个主机 × 4个基准 × 12+方法 = 约150+条记录，报告绝对改进值。
  - **消融实验**（图3）：在KodCode和TriviaQA上对AutoGen和CAMEL进行4种变体消融（无图扩展、无协调、无失败反思、无失败准入），结果采用柱状图。
  - **记忆库动态分析**（图4）：展示活跃卡片数、负向卡片比例、边类型分布。
  - **协调压缩效率**（图5）：显示扩展候选被剪枝的比例（＞50% KodCode，＞80% PDDL）。
  - **案例分析**（图6）：TriviaQA失败预防案例，展示局部关系子图。
  - **附录子图**（图7-图10）：所有基准和主机的卡片库子图，验证跨主机一致性和关系结构。
- **充分性与公平性**：
  - 严格控制：主机架构、LLM、解码设置、任务顺序、提示预算固定，仅记忆机制不同。
  - 预准序协议确保训练/测试分离。
  - 检索型QA使用相同外部检索器，不调优记忆控制器。
  - 但未报告标准差/置信区间（可能因确定性解码），且部分基线数据复制自其他论文（注*），需确认复现一致性。整体实验设计严谨，覆盖广泛，消融完整。

### 6. 主要结论与发现

- **ConMem在所有主机和基准上实现一致正向提升**，跨主机平均提升10.9~12.9绝对百分点（相对于No-memory）。
- **排名**：在15个主机-指标比较中ConMem第一8次、第二7次；**优于可学习方法LatentMem**（Average高出0.95-1.81点）、**匹配或略超动态方法ReMe**（高0.02-0.87点）。
- **协调对性能关键**：在KodCode和PDDL这类依赖关系密集的任务上提升最大，QA上提升较窄但一致。消融表明**移除协调导致最大下降**。
- **效率提升**：协调阶段剪枝超过50%的扩展候选（PDDL上＞80%），说明瓶颈不在召回数量而在召回一致性。
- **记忆库结构**：支持/满足边占主导，但代码和规划任务中约束/冲突边更多，需要主动协调。
- **核心设计合理**：卡片作为可复用策略单元，需同时是记忆单元和技能单元。

### 7. 优点

- **训练无关**：无需调整模型权重或主机调度，仅在提示层面进行控制，轻量级且易于部署。
- **结构化记忆**：卡片具有类型化字段（state/plan/exec/eval）和符号，可检查、可协调、可复用且包含失败线索。
- **关系感知协调**：通过类型化图在注入前解决冲突、恢复依赖、去除冗余，避免将矛盾传给主机LLM隐式处理。
- **预算化控制**：严格的Token预算管理，确保记忆前缀长度受控，避免膨胀；同时提供量化风险保证（附录E.7）。
- **理论支撑**：给出了需求估计遗憾界、负向记忆激活阈值、卡片充分性定理、图扩展为依赖闭包、协调为团塌缩等理论结果，为设计提供合理性。
- **广泛实验验证**：多基准、多主机、多对比方法，消融充分，分析深入（记忆库动态、压缩效率、案例子图）。

### 8. 不足与局限

- **控制器质量依赖LLM**：卡片提取、失败反思、关系判断均需LLM调用，这些阶段可能引入误差并级联到后续检索和协调。实验未专门分析这类误差的影响。
- **阈值校准未完全自动化**：虽然声称使用配置文件和分布分位数（附录E.7），但具体如何从校准统计中确定`λ`权重、`θ`阈值等仍需人工设计，并非完全端到端自适应。
- **无法超越主机能力**：ConMem只能改进已有策略的复用和避免重犯，但不能赋予主机原本不具备的技能（如基于训练的推理链或工具使用）。对于主机本身不擅长的任务，提升有限。
- **实验覆盖局限**：仅使用一个LLM（Qwen3-4B），未验证在不同规模模型（如7B、70B、开源/闭源）上的泛化性；PDDL规模较小（60任务），可能统计不够稳定；未涉及多轮对话、工具使用等更复杂的MAS场景。
- **计算开销**：虽然提示预算减小，但卡片更新和关系图维护本身需要LLM调用，随着任务数量增长可能有累积成本。论文未对比总时间或Token消耗。
- **缺乏不确定性度量**：报告为点估计，未提供多次运行的标准差，尽管确定性解码可能使结果稳定，但仍需更多重复验证。
- **实现细节开放**：附录提供了 prompts，但完整代码在匿名仓库，复现需依赖自描述性，可能存在未披露的预处理或后处理步骤。

（完）
