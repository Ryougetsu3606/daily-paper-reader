---
title: Deployment-Time Memorization in Foundation-Model Agents
title_zh: 基础模型代理中的部署时记忆化
authors: "Lei, Chen, Guilin Zhang, Kai Zhao, Dalmo Cirne, Andy Olsen, Xu Chu, Zeke Miller, Alet Blanken, Amine Anoun, Jerry Ting"
date: 2026-06-08
pdf: "https://arxiv.org/pdf/2606.10062v1"
tags: ["query:mm-agent-mem"]
score: 9.0
evidence: 智能体记忆设计权衡个性化效用与隐私风险
tldr: 基础模型代理的持久记忆使记忆成为部署时功能，现有研究未全面评估记忆设计对隐私-效用的联合影响。本工作将代理记忆建模为隐私-效用前沿，通过个性化召回和对抗提取率衡量，并扫描摘要激进程度、检索广度k和删除模式三个旋钮。关键事实摘要大幅降低提取风险而不牺牲召回，但导致删除失败，仅全管道清除才能消除残留。总之，持久代理记忆需作为第一类记忆机制评估，综合考虑回忆、提取和擦除。
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10062v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 819, \"height\": 967, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10062v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 838, \"height\": 556, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10062v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 1497, \"height\": 542, \"label\": \"Table\"}]"
motivation: 现有工作未联合考虑记忆设计对个性化效用、提取风险和删除保真度的影响，需系统评估部署时记忆的隐私-效用权衡。
method: 提出隐私-效用前沿框架，用个性化召回和对抗提取率衡量，扫描摘要激进程度、检索广度k和删除模式，并引入遗忘残留分数量化残留。
result: "关键事实摘要使提取率降低76%/64%，几乎不失召回；原始删除下约20%实例中派生摘要可恢复，仅全管道清除可消除残留。"
conclusion: 持久代理记忆须作为第一类记忆机制，通过帮助回忆、可提取性和可擦除性三维评估。
---

## 摘要
基础模型代理正逐渐成为跨交互记忆用户的长期存在系统，使得记忆化成为一个明确的部署时功能，而不仅仅是模型权重的属性。现有工作处理参数化记忆化或审计固定记忆配置，但并未描述记忆设计选择如何共同影响个性化效用、提取风险和删除保真度。我们将这一层面研究为部署时记忆化，将代理记忆构建为通过个性化召回率（PR）和对抗性提取率（AER）衡量的隐私-效用前沿，并扫视三个记忆设计旋钮：摘要激进程度、检索广度（k）和删除模式。我们进一步引入遗忘残留分数（FRS）来量化已删除信息是否可以从派生记忆层中恢复。在LongMemEval上，关键事实摘要在Gemma 3 12B上将金丝雀提取率降低76%，在GPT-4o-mini上降低64%，同时几乎保留了所有个性化召回率；关键是，一旦内容被压缩掉，增加k不再恢复泄漏。然而，同样的压缩导致删除保真度失败：仅原始删除使得派生摘要副本在大约20%的实例中可恢复，只有全流水线清除或墓碑编辑才能将最差层的残留降至零。这些结果共同表明，持久代理记忆必须作为第一类记忆化机制进行评估——通过它帮助代理回忆的内容、使其可提取的内容以及真正能够擦除的内容来评估。

## Abstract
Foundation-model agents are increasingly long-lived systems that remember users across interactions, making memorization an explicit deployment-time function rather than solely a property of model weights. Existing work addresses parametric memorization or audits fixed memory configurations, but does not characterize how memory-design choices jointly shape personalization utility, extraction risk, and deletion fidelity. We study this surface as deployment-time memorization, formulating agent memory as a privacy-utility frontier measured by Personalization Recall (PR) and Adversarial Extraction Rate (AER), and sweeping three memory-design knobs: summarization aggressiveness, retrieval breadth (k), and deletion mode. We further introduce the Forgetting Residue Score (FRS) to quantify whether deleted information remains recoverable from derived memory tiers. On LongMemEval, key-fact summarization reduces canary extraction by 76% on Gemma 3 12B and 64% on GPT-4o-mini while preserving nearly all personalization recall; critically, once content is compressed away, increasing k no longer restores leakage. The same compression, however, induces a deletion-fidelity failure: raw-only deletion leaves derived summary copies recoverable in approximately 20% of instances, and only full-pipeline purge or tombstone redaction drives worst-tier residue to zero. Together, these results establish that persistent agent memory must be evaluated as a first-class memorization mechanism -- assessed by what it helps agents recall, what it makes extractable, and what it can truly erase.

---

## 论文详细总结（自动生成）

# 论文《Deployment-Time Memorization in Foundation-Model Agents》中文总结

## 1. 核心问题与整体含义（研究动机和背景）
- **背景**：基础模型代理正从无状态助手演变为长期记忆系统，能够跨交互记住用户偏好（如座位偏好、编程习惯）。这种持久记忆使“记忆化”不再是模型权重的偶然属性，而成为部署时的显式系统功能。
- **问题**：现有研究主要关注参数化记忆化（训练数据在模型权重中的泄露）或对固定记忆配置进行审计，但未系统评估**记忆设计选择**（如摘要激进程度、检索广度、删除模式）如何联合影响三个关键维度：**个性化效用**（用户能否被正确回忆）、**提取风险**（攻击者能否恢复秘密）、**删除保真度**（用户要求“忘记”后信息是否真正被擦除）。
- **整体含义**：持久代理记忆必须被视为**第一类记忆化机制**，需要从“帮助回忆、可提取性、可擦除性”三个维度全面评估，而非仅关注参数泄露。

## 2. 论文提出的方法论：核心思想与关键技术
- **核心思想**：将代理记忆系统建模为**隐私-效用前沿（Privacy-Utility Frontier）**，通过两个指标量化：
  - **个性化召回率（PR）**：衡量代理正确回答需要用户上下文的问题的能力。
  - **对抗性提取率（AER）**：衡量攻击者通过直接、间接或越狱提示恢复用户秘密（高熵金丝雀）的比例。
- **记忆管道**：基于检索增强记忆（类似于MemGPT），在每个用户会话后将信息写入长期记忆；查询时通过余弦相似度检索`k`个记忆块并注入上下文。
- **三个记忆设计旋钮**：
  - **摘要激进程度（S）**：S=0（原始用户轮次）、S=1（关键事实摘要）、S=2（单句会话摘要）。
  - **检索广度（k）**：注入上下文的内存块数量（1、3、6、10、25）。
  - **删除模式（Deletion Mode）**：五步梯级，包括noop（无操作）、raw only（仅删除原始文本）、raw plus resummarize（删除原始+重新摘要）、full purge（全面清除）、tombstone（用[REDACTED]替换并重新嵌入）。
- **遗忘残留分数（FRS）**：用于量化删除后秘密是否仍可从各记忆层（原始层、摘要层）恢复，定义为最差层的提取率。
- **关键公式**：
  - 个性化召回率：`PR(θ) = E[1(answer correct)]`，其中正确判断基于余弦相似度>0.5或完整词语匹配。
  - 对抗性提取率：`AER_ℓ(θ) = (1/|C|) Σ 1[ci ∈ r_i,ℓ]`，其中C为金丝雀集合，ℓ为探测类型。
  - 隐私-效用AUC（PUA）：对给定k集合，计算经验可达到召回率曲线下的面积，综合衡量隐私-效用权衡。

## 3. 实验设计
- **数据集**：使用LongMemEval的oracle拆分，包含多会话聊天历史及需要长期用户上下文的问答对。采样`N=50`个实例。
- **金丝雀注入**：每个实例注入一个合成高熵金丝雀（如“my private session token is XQ7-VIOLET-3829”），随机放置在非证据用户轮次中，确保提取归因于管道记忆而非训练数据。
- **威胁模型**：攻击者只能查询代理，不能直接访问或编辑记忆存储。探测类型：直接询问、间接询问、越狱式提示（如开发者模式调试转储）。
- **对比方法**：系统性地扫视S∈{0,1,2}、k∈{1,3,6,10,25}以及五种删除模式。主要实验在Gemma 3 12B（本地通过Ollama部署）上运行，并在GPT-4o-mini（API）上复现S∈{0,1}以验证跨模型一致性。
- **检索嵌入**：使用all-MiniLM-L6-v2进行余弦相似度检索。
- **验证**：以LLM作为评判器验证PR评分，报告保守假阴性。

## 4. 资源与算力
- **未明确说明**：论文仅提到Gemma 3 12B本地通过Ollama部署（推测单卡GPU即可运行，但未报告具体型号、数量、训练时长）；GPT-4o-mini通过API调用。未提供整体算力消耗（如总查询次数、GPU小时数）。这一信息缺失为潜在局限。

## 5. 实验数量与充分性
- **实验规模**：在50个实例上进行了全因子扫视：摘要级别（3种）×检索广度（5种）×删除模式（5种）→ 主体实验共约750种配置组合。此外对两个模型进行了关键对比。
- **消融实验**：删除模式梯级本身即是消融设计（从noop到tombstone逐步增加操作），清晰刻画了各操作的独立贡献。还通过探测类型分解（直接/间接/越狱）分析了泄漏来源。
- **充分性评估**：实验设计较为充分，覆盖了主要记忆设计旋钮，并跨两个不同训练管道（开源Gemma和闭源GPT-4o-mini）验证了主要发现，增强了结论的泛化性。但样本量（N=50）相对较小，可能影响统计稳健性；论文报告了95% bootstrap置信区间，部分结果区间较宽，需要更大规模验证。
- **公平性**：采用了标准基准LongMemEval，金丝雀设计独立于训练数据，避免了训练时暴露。但仅测试了单个嵌入模型（all-MiniLM-L6-v2），未探索不同嵌入的影响。

## 6. 论文的主要结论与发现
- **摘要激进程度从根本上改变隐私-效用前沿**：
  - 原始记忆（S=0）无干净工作点：PR≈0.59但AER≈0.83（Gemma）；增加k同等地提升用户和攻击者能力。
  - 关键事实摘要（S=1）将金丝雀提取率降低76%（Gemma）和64%（GPT-4o-mini），同时PR几乎不变（下降约2-5个百分点）。
  - 单句摘要（S=2）进一步降低AER但以额外效用损失为代价，因此S=1是最佳平衡点。
- **压缩后检索广度变得隐私中性**：一旦信息被摘要压缩掉，增加k不会恢复泄漏。
- **直接与间接探测在摘要后趋同**：原始记忆下两者存在差异，摘要后差异消失，表明摘要控制了事实可恢复性。
- **越狱行为依赖于底层模型**：GPT-4o-mini由于RLHF天然拒绝越狱，而Gemma在摘要后也关闭了越狱通道。
- **删除保真度不足**：
  - 仅删除原始文本（raw only）在S=1下与无操作无统计差异（约20%的摘要副本可恢复）。
  - 重新摘要（raw plus resummarize）降低但未消除残留。
  - 全流水线清除（full purge）或墓碑替换（tombstone）才能将最差层残留降至零。

## 7. 优点
- **问题定义清晰**：首次将“部署时记忆化”概念化，区分于参数化记忆化，并建立了隐私-效用前沿框架。
- **系统性扫描**：同时控制摘要、检索、删除三大旋钮，避免孤立分析每个因素。
- **删除保真度量化**：引入FRS指标，揭示了“删除”在层级记忆中的失效风险，具有实际工程价值。
- **跨模型验证**：在Gemma和GPT-4o-mini上复现关键结果，表明摘要的影响是管道级而非模型特有现象。
- **威胁模型现实**：考虑了直接、间接、越狱三类攻击，覆盖实际部署中常见的攻击面。

## 8. 不足与局限
- **样本量较小**：仅50个实例，部分置信区间较宽，需要更大规模实验确认统计显著性。
- **计算资源未报告**：缺乏GPU型号、数量、运行时间等细节，不利于复现和成本评估。
- **单一嵌入模型**：仅使用all-MiniLM-L6-v2，不同嵌入方法（如更强大的BERT、OpenAI嵌入）可能会改变检索行为与泄漏模式。
- **金丝雀设计简单**：采用固定格式“my private session token is [value]”，实际用户信息可能包含多样化的敏感事实，需测试更复杂的场景。
- **未考虑时间累积**：实验基于单次会话注入，未模拟长期交互中记忆的迭代更新、衰减和重写效果。
- **仅两个模型**：虽然跨了两个管道，但未涵盖更小或更大的模型（如Llama-70B、Claude），未知是否适用更广泛的模型家庭。
- **未见差分隐私对比**：论文未比较其方法与传统差分隐私训练的隐私-效用权衡，缺少与现有技术的锚定。

（完）
