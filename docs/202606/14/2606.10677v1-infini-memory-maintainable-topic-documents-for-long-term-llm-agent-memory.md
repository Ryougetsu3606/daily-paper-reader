---
title: "Infini Memory: Maintainable Topic Documents for Long-Term LLM Agent Memory"
title_zh: Infini Memory：面向长期LLM智能体的可维护主题文档记忆
authors: "Suozhao Ji, Baodong Wu, Zehao Wang, Lei Xia, Qingping Li, Ruisong Wang, Wenbo Ding, Zhenhua Zhu, Boxun Li, Guohao Dai, Yu Wang"
date: 2026-06-09
pdf: "https://arxiv.org/pdf/2606.10677v1"
tags: ["query:mm-agent-mem"]
score: 9.0
evidence: 基于主题文档的长时期LLM智能体记忆架构；可维护持久记忆
tldr: "现有LLM代理长期记忆系统常以孤立记录、摘要或索引片段存储观测结果，导致证据聚合、事实修订和内存维护困难。本文提出Infini Memory，一种可维护的基于文本的持久内存架构，将代理内存组织为主题结构化文档，每个主题文档作为语义单元收集相关证据并随时间修订事实。新观测先暂存缓冲区，定期整合为连贯文本；推理时通过迭代工具调用让LLM读取内存。在MemoryAgentBench上达到64.7%总体得分，消融实验表明主题结构化维护和迭代证据检查互补提升了长期内存使用的不同方面。"
source: arxiv
selection_source: fresh_fetch
figures_json: "[{\"url\": \"assets/figures/arxiv/2606.10677v1/fig-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 803, \"height\": 524, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10677v1/fig-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 773, \"height\": 829, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10677v1/fig-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 794, \"height\": 758, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10677v1/fig-004.webp\", \"caption\": \"\", \"page\": 0, \"index\": 4, \"width\": 795, \"height\": 587, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10677v1/fig-005.webp\", \"caption\": \"\", \"page\": 0, \"index\": 5, \"width\": 793, \"height\": 643, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10677v1/fig-006.webp\", \"caption\": \"\", \"page\": 0, \"index\": 6, \"width\": 788, \"height\": 897, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10677v1/fig-007.webp\", \"caption\": \"\", \"page\": 0, \"index\": 7, \"width\": 783, \"height\": 440, \"label\": \"Figure\"}, {\"url\": \"assets/figures/arxiv/2606.10677v1/fig-008.webp\", \"caption\": \"\", \"page\": 0, \"index\": 8, \"width\": 794, \"height\": 794, \"label\": \"Figure\"}]"
tables_json: "[{\"url\": \"assets/tables/arxiv/2606.10677v1/table-001.webp\", \"caption\": \"\", \"page\": 0, \"index\": 1, \"width\": 1659, \"height\": 613, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10677v1/table-002.webp\", \"caption\": \"\", \"page\": 0, \"index\": 2, \"width\": 973, \"height\": 372, \"label\": \"Table\"}, {\"url\": \"assets/tables/arxiv/2606.10677v1/table-003.webp\", \"caption\": \"\", \"page\": 0, \"index\": 3, \"width\": 516, \"height\": 301, \"label\": \"Table\"}]"
motivation: 现有长期记忆系统难以聚合证据、修订事实，且可维护性差。
method: 将内存组织为主题文档，新观测暂存缓冲区并定期整合；推理时通过迭代工具调用实现智能检索。
result: "在MemoryAgentBench上取得64.7%总体得分，消融实验验证了主题维护与迭代检查的有效性。"
conclusion: 主题结构化和迭代证据检查是提升LLM长期记忆可维护性和证据聚合能力的关键技术。
---

## 摘要
长期LLM智能体需要持久记忆来追踪变化的事实，并在会话间提供相关证据。现有记忆系统通常将观察结果存储为孤立记录、摘要或索引片段，这使得证据聚合、事实修正和记忆维护变得困难。我们提出Infini Memory，一种可维护的基于文本的持久记忆架构，将智能体记忆视为主题结构化文档。每个主题文档作为一个语义单元，用于收集相关证据、保留元数据并随时间修正事实。新观察结果首先暂存于缓冲区，并定期整合为连贯的文本上下文。在推理时，一种智能体检索过程让LLM通过迭代工具调用来读取记忆，而非单次检索步骤。在MemoryAgentBench上，Infini Memory取得了64.7%的总分。消融实验表明，主题结构化维护和迭代证据检查改善了长期记忆使用的互补方面。

## Abstract
Long-term LLM agents need persistent memory that can track changing facts and provide relevant evidence across sessions. Existing memory systems often store observations as isolated records, summaries, or indexed fragments, which makes evidence aggregation, fact revision, and memory maintenance difficult. We propose Infini Memory, a maintainable text-based persistent memory architecture that treats agent memory as topic-structured documents. Each topic document serves as a semantic unit for collecting related evidence, preserving metadata, and revising facts over time. New observations are first staged in a buffer and periodically consolidated into coherent textual contexts. At inference time, an agentic retrieval procedure lets the LLM read memory through iterative tool calls rather than a single retrieval step. On MemoryAgentBench, Infini Memory achieves 64.7% overall score. Ablations show that topic-structured maintenance and iterative evidence inspection improve complementary aspects of long-term memory use.

---

## 论文详细总结（自动生成）

好的，以下是对论文《Infini Memory: Maintainable Topic Documents for Long-Term LLM Agent Memory》的详细中文总结。

### 1. 论文的核心问题与整体含义（研究动机和背景）

- **核心问题**：现有的大语言模型（LLM）长期记忆系统在处理持续性交互时存在四个核心缺陷：
    1.  **记忆碎片化 (Memory Fragmentation)**：关于同一用户、任务或事件的证据分散在众多孤立的小记录中，难以聚合。
    2.  **记忆冲突 (Memory Conflict)**：更新后的信息与旧信息共存，导致检索时返回矛盾的内容。
    3.  **压缩损失 (Compression Loss)**：为节省空间而对长历史进行摘要化，会丢失时间顺序、来源线索等关键上下文。
    4.  **检索不足 (Insufficient Retrieval)**：基于向量相似度或固定 top-k 的单次检索方法，往往只返回碎片化信息，不足以支持复杂的多跳推理。
- **整体含义**：论文指出，长期记忆应被视为一个**生命周期维护问题**，而不仅仅是数据存储。一个良好的记忆系统需要支持三个耦合操作：**写入（Write）** 持久化信息、**维护（Maintain）** 分组证据、解决冲突、保留上下文，以及**读取（Read）** 检索足够上下文而非孤立碎片。

### 2. 论文提出的方法论

- **核心思想**：将LLM智能体的持久记忆表示为**可维护的主题文档（Topic Documents）**。每个主题文档是一个语义单元，用于收集、组织和修订关于同一主题的相关证据，同时保留条目级别的元数据。
- **关键技术细节**：
    1.  **主题文档表示 (Topic Document Representation)**：
        - 每个文档包含**元数据**（ID、摘要、Token数、创建时间、更新日志等）和**结构化正文**。
        - 正文由层级标题和记忆条目组成，每个条目带有解析签名 `＜seq＝序号, time＝时间, source＝来源＞`，保留了时间、顺序和来源信息，便于后续修订。
    2.  **缓冲写入与整合（Buffered Writing and Consolidation）**：
        - 设立短期缓冲区 `CURRENT`，新提取的记忆先追加到缓冲区，避免频繁修改主题库。
        - 当缓冲区大小或时间达到阈值时，触发**重写（Rewrite）** 操作，将缓冲区内容去重、解决矛盾、格式化，生成一个标准化草稿。
        - **规划器（Planner）** 决定将标准化草稿的每个模块更新到现有主题文档（处理更新和冲突）还是创建新文档。之后清空缓冲区。
        - **定期维护**：对过大的文档进行**拆分（Split）**，对过小且主题相似的文档进行**合并（Merge）**，并刷新元数据。
    3.  **智能体检索（Agentic Retrieval）**：
        - LLM 作为检索智能体，通过**迭代调用一系列工具**来搜索和验证证据。
        - 工具包括：全局文本搜索 (`grep`)、文档内搜索 (`grep_doc`)、目录搜索 (`search`)、目录浏览 (`list_docs`)、范围行读取 (`read_lines`)。
        - LLM 自主决定调用哪个工具、何时停止搜索，并进行证据的局部上下文扩展。
        - 如果智能体检索证据不足，会使用 **BM25** 作为备选检索。
- **算法流程：** (文字描述)
    1.  从与用户的交互中提取可持久化的记忆片段。
    2.  将提取的记忆追加到 `CURRENT` 缓冲区。
    3.  如果缓冲区超阈，则重写并整合内容。
    4.  规划器将整合后的内容路由到现有的主题文档或创建新文档。
    5.  定期执行拆分/合并维护操作。
    6.  **推理时**：LLM 接收用户查询和文档目录，开始工具调用循环。
    7.  LLM 调用搜索、文档内查找或行读取工具，逐步缩小范围并检查结果。
    8.  当收集到足够证据或达到步数限制时，停止工具调用。
    9.  将最终证据与 `CURRENT` 缓冲区内容一同提供给 LLM，生成最终答案。

### 3. 实验设计

- **基准与场景**：使用 **MemoryAgentBench** 作为主要评测基准。该基准评估了四种长期记忆能力：
    - **准确检索 (AR)**：从长历史中回忆事实。
    - **测试时学习 (TTL)**：在上下文中获取新规则。
    - **长程理解 (LRU)**：对扩展叙事进行理解。
    - **选择性遗忘 (SF)**：更新过时信息的能力。
- **对比方法**：与七个基线方法进行比较：RAPTOR, MemoRAG, HippoRAG-v2, Mem0, MemGPT, LightMem, REMem。
- **模型与配置**：所有方法使用 `gpt-5-mini` 作为基座模型，输入块大小为 4096 个 token。评估使用 `gpt-5` 作为裁判，进行二值正确性判断。

### 4. 资源与算力

- **论文未明确说明**：论文本身未提及训练模型或运行实验所需的GPU型号、数量和时长。所有方法均基于 `gpt-5-mini` 的API进行推理，因此算力消耗主要体现在API调用次数和推理token数量上。

### 5. 实验数量与充分性

- **实验组数**：实验设计较为充分，主要包含：
    - **主实验结果**（Table 1）：在MemoryAgentBench的四个子能力（AR, TTL, LRU, SF）及9个更细分的子数据集上与7个基线进行全面对比。
    - **消融实验**：
        1.  **结构维护消融（Table 2, Figure 6）**：在固定检索策略（混合模式）下，移除主题文档的拆分和合并操作，观察性能下降（从76.0%降至69.3%）。
        2.  **检索策略消融（Table 2, Figure 7）**：在固定记忆维护策略下，对比单次摘要选择、混合检索（摘要+BM25）和智能体检索的性能（从41.7%提升至79.3%）。
        3.  **文档拆分阈值灵敏度分析（Table 3, Figure 8）**：系统性地测试了不同token阈值（1000-9000）对性能的影响，并分析了文档数量与阈值的关系。
- **充分性与公平性**：实验充分，消融实验清晰地分离了各个模块（维护和检索）的贡献，有效性高。与7个基线在相同模型和配置下对比，保证了客观性和公平性。但仅在一个基准（MemoryAgentBench）上评测，泛化性有待验证。

### 6. 论文的主要结论与发现

- **主要结论**：Infini Memory 在 MemoryAgentBench 上取得了最高的 **64.7%** 总体得分，远超最强基线（HippoRAG-v2，45.5%），主要优势体现在**选择性遗忘（SF）** 和**长程理解（LRU）** 任务上，这得益于其对事实进行有效结构化和时间追踪的能力。
- **关键发现**：
    1.  **主题文档维护**和**智能体检索**是互补的。消融实验显示，移除结构维护会损失6.7个点，而将混合检索升级为智能体检索则能提升3.3个点。二者结合方能达到最佳效果。
    2.  **多跳选择性遗忘（FC-MH）** 是当前架构的瓶颈，得分远低于单跳（FC-SH）。因为一个推理链可能涉及在不同时间更新的多个文档，单次重写无法保证跨文档的一致性。

### 7. 优点

- **方法设计亮点**：
    - **文档主题化（Topic Structuring）**：将碎片化信息组织成结构化文档，有效缓解了记忆碎片化和冲突问题，并保留了关键元数据。
    - **异步维护（Buffered & Async Maintenance）**：通过缓冲区和定期整合，将高频写入与低频维护分离，既保证了写入效率，又确保了记忆的长期可维护性和一致性。
    - **智能体检索（Agentic Retrieval）**：让LLM主动控制检索过程，可以迭代、有目的地寻找和验证证据，解决了单次检索不足的缺点，提升了检索的上下文丰富度和准确性。
    - **内存状态可解释、可编辑**：使用纯文本文档作为存储后端，使记忆状态对用户和开发者完全透明、可审计、可手动编辑，降低了部署和调试的复杂度。
- **实验设计亮点**：通过精心设计的消融实验，解耦了“维护”和“检索”两个核心机制的贡献，验证了各自的有效性，论证清晰有力。

### 8. 不足与局限

- **多跳选择性遗忘的难题**：论文承认，在涉及跨多个主题文档的复杂推理时（如FC-MH任务），当前的写入时整合（write-time consolidation）无法保证跨文档的一致性，导致性能下降。这是一个固有局限，也是未来工作的方向。
- **计算开销**：智能体检索在多轮工具调用中会消耗更多的 API Token 和计算时间，相比单次检索的开销更大。论文虽然讨论了这个问题，但未提供详细的效率对比分析。
- **对LLM能力的依赖**：系统的核心操作，如记忆提取、重写、规划、检索，都高度依赖底层LLM（如 `gpt-5-mini`）的能力。如果LLM能力不足，整个记忆系统的表现可能会严重受限。
- **实验范围局限**：仅在 MemoryAgentBench 一个基准上进行评估，虽然该基准针对性很强，但缺乏在更广泛、更真实的应用场景（如对话、编程、工具使用）中的验证。
- **缺少关键性能指标**：论文未分析检索的 Hit Rate 等核心指标，仅使用了端到端的任务完成准确率，这使得难以判断性能瓶颈究竟是在*检索不到正确文档*，还是在*检索到后无法理解或利用*。

好的，根据您的要求，我将从上一次输出的中断处（即“不足与局限”部分之后）继续补全，增加论文中隐含的未来工作方向以及一个简要的总结与评价。请注意，以下内容不会重复之前已经输出的任何部分。

### 9. 未来工作方向

- **跨文档一致性维护**：针对多跳选择性遗忘（FC-MH）的瓶颈，论文提出未来需探索更高级的全局一致性维护机制。例如，在重写或整合时，通过因果推理链追踪，自动检测并协调多个相关主题文档中的冲突，实现跨文档的原子化更新。
- **计算效率优化**：智能体检索虽提升了召回质量，但多轮工具调用增加了LLM API消耗和延迟。未来可尝试：
  - 引入预检索的混合索引（如结合BM25与向量库的快速预筛选），减少智能体需要搜索的文档范围。
  - 设计更高效的搜索终止条件，基于信息增益或置信度阈值提前停止。
- **底层LLM无关性**：探索将记忆模块的核心操作（提取、重写、规划）蒸馏或迁移到更小、更专用的模型上，降低对大型LLM（如gpt-5-mini）的依赖，提升系统的部署灵活性和成本效益。
- **更广泛的基准评估**：在更多样化的真实场景（如客服对话、个人助理、复杂工具使用、游戏NPC记忆）中测试Infini Memory的泛化能力，并引入人工评估维度（如用户满意度、记忆一致性）。

### 10. 总结与评价

Infini Memory 通过将长期记忆建模为**可维护的主题文档**，结合**缓冲写入与异步整合**以及**智能体驱动的迭代检索**，系统性地解决了LLM长期记忆中的碎片化、冲突、压缩损失和检索不足四大缺陷。论文方法设计清晰、模块化，消融实验有力地证实了“维护”与“检索”两个组件的互补价值。尽管存在多跳一致性维护困难、计算开销较大等局限，但该工作为长期记忆系统设计提供了一个务实且高效的新范式——将记忆视为一个**生命周期管理问题**，而非简单的存储问题。其实验结果（在MemoryAgentBench上领先最强基线19.2个点）充分展示了该方法在复杂记忆任务上的优势，对后续智能体记忆研究具有重要的参考意义。

（完）
